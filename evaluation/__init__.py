"""
Evaluation utilities and scripts.

Note: We keep this folder importable so `hrsa` and other code can rely on
`evaluation.common` without needing `sys.path` hacks.
"""

from __future__ import annotations
